# 项目结构与功能说明

这份文档只保留对公开仓库有价值的信息：主要目录做什么、页面从哪里进、核心功能落在哪些模块。

## 项目定位

AnimeTrack 当前是一个本地优先桌面应用。

- 桌面壳：Tauri 2
- 前端：React + TypeScript + Vite
- 路由：React Router
- 本地数据：SQLite
- 机密信息：系统安全存储优先

它的重点不是在线部署，而是把个人追番记录稳定保存在本机，并提供备份、恢复和可选 AI 辅助。

## 主要目录

### 根目录

- README.md：对外说明文档，介绍应用用途和页面
- 项目结构与功能说明.md：这份结构说明
- package.json：前端脚本和依赖入口
- vite.config.ts：Vite 构建配置
- tsconfig.json：前端 TypeScript 配置
- tsconfig.node.json：Vite / Node 侧 TypeScript 配置

### 前端页面与组件

- src/main.tsx：React 启动入口，只负责挂载 App 和全局样式
- src/App.tsx：桌面端主路由入口，负责 HashRouter、主题、Toast、数据预热和页面懒加载
- src/pages：当前桌面端真正的路由页，后续页面级优化优先落这里
- src/features：按业务拆出来的共享模块，适合继续承接页面内部逻辑
- components：复用的展示组件和 Dashboard 相关视图，整体偏 UI / 展示层
- app/anime：当前只保留 atlas、seasons 两个分析页；不再作为主页面目录继续扩张

### 前端数据与服务

- src/lib/runtime.ts：运行时能力探测
- src/lib/anime-store.ts：番剧、历史、导入初始化和缓存同步的主仓储入口；现在是前端最重的一层
- src/lib/settings-store.ts：设置页本地持久化入口
- src/lib/backup-store.ts：备份、导出、恢复服务
- src/lib/secret-store.ts：安全存储相关前端封装
- src/lib/quick-record.ts：一句话快速录入
- src/lib/anime-metadata-enrichment.ts：AI / 元数据补充
- lib：跨页面复用的纯工具与类型，例如 theme、local-date-time、dashboard-shared、anime-preference-insights

### Tauri 与本地仓储

- src-tauri/src/lib.rs：桌面命令注册与主要 Tauri 命令入口
- src-tauri/src/storage.rs：SQLite 仓储实现
- src-tauri/src/secure_storage.rs：机密信息安全存储实现
- src-tauri/tauri.conf.json：桌面窗口、打包和应用信息配置
- src-tauri/icons：桌面图标资源
- src-tauri/wix/main.wxs：Windows MSI 安装器模板

### 其他目录

- hooks：Dashboard 和分析页仍在复用的数据聚合 hooks
- database：历史 SQL 和迁移脚本参考
- scripts：当前保留的打包和数据辅助脚本
- docs-private：迁移、打包、验证等内部记录

## 当前真实分层

当前代码已经不是 Next.js App Router 应用，而是 Vite + React Router 桌面前端。可以按下面的方式理解：

- 路由层：src/App.tsx、src/pages、app/anime/atlas/page.tsx、app/anime/seasons/page.tsx
- 业务共享层：src/features
- 展示组件层：components
- 状态与持久化层：src/lib
- 纯工具与类型层：lib、src/types.ts
- 桌面能力层：src-tauri

后续做优化时，建议优先遵守下面这条边界：

- 页面路由和页面组装放 src/pages
- 页面专属但可复用的业务逻辑放 src/features
- 纯展示组件继续放 components
- 与存储、Tauri、AI、备份直接打交道的逻辑继续收敛到 src/lib
- 纯格式化、主题、类型、无副作用工具放 lib

## 这次整理后的结论

当前目录结构整体是能工作的，但存在两种历史包袱：

- 一类是 Next 风格残留命名还在，例如 app/page 风格文件和 next/link、next/image 兼容层；这部分现在是“兼容运行”，不是主框架
- 一类是迁移后没再接线的壳层；本次已移除未接入文件和空目录，避免继续误导后续开发

本次确认并已清理：

- components/Providers.tsx：残留 next-auth Provider，当前无任何入口引用
- components/shared/RouteLoadingBar.tsx：依赖 next/navigation，当前无任何入口引用
- src/styles.css：当前未被入口加载
- app/admin：空目录
- app/api：空目录骨架

当前仍然保留但属于“兼容层”的内容：

- vite.config.ts 中 next/link、next/image、next/dynamic 的 alias
- components 和 app/anime 下部分组件仍使用 Next 风格 import，但实际由 src/compat 兼容层兜底

这层兼容目前是有作用的，不能直接删；如果后续要继续收口，建议逐步把这些 import 改成 react-router-dom 和原生 img / React.lazy 再移除 alias。

## 页面与源码对应关系

### 主页概览

- 路由：/#/
- 入口：components/Dashboard.tsx
- 作用：展示总览统计、近期活动和重点作品

### 番剧列表

- 路由：/#/anime
- 入口：src/pages/AnimePage.tsx
- 作用：搜索、筛选、添加、编辑、删除番剧记录

### 番剧详情

- 路由：/#/anime/:id
- 入口：src/pages/AnimeDetailPage.tsx
- 作用：展示单部作品详情、历史、备注和元数据补充

### 时间线

- 路由：/#/anime/timeline
- 入口：src/pages/AnimeTimelinePage.tsx
- 作用：按时间浏览观看记录

### 作品元数据图谱

- 路由：/#/anime/atlas
- 入口：app/anime/atlas/page.tsx
- 作用：查看标签、评分、集数、声优等分析视图

### 开播季度回顾

- 路由：/#/anime/seasons
- 入口：app/anime/seasons/page.tsx
- 作用：按季度回看片单分布

### 设置

- 路由：/#/settings
- 入口：src/pages/SettingsPage.tsx
- 作用：管理显示名称、主题与 AI 配置

### 备份与导出

- 路由：/#/backup
- 入口：src/pages/BackupPage.tsx
- 作用：导出 JSON / CSV、本地快照、导入恢复

### 数据管理

- 路由：/#/admin
- 入口：src/pages/AdminPage.tsx
- 作用：集中管理条目和观看记录

## 核心功能模块

### 1. 本地番剧仓储

核心目标是把番剧条目和观看历史保存在本机，而不是依赖服务器。

- 前端读取入口：src/lib/anime-store.ts
- Rust 持久化实现：src-tauri/src/storage.rs
- 存储介质：SQLite

### 2. 设置与机密信息

普通设置和机密配置拆成两条路径：

- 普通设置：主题、显示名称等，走 settings store
- 机密信息：例如 AI Key，优先写入系统安全存储

### 3. 备份与恢复

备份功能支持：

- 本地快照
- JSON 导出
- CSV 导出
- 导入恢复

主要入口在 src/lib/backup-store.ts。

### 4. AI 辅助

AI 目前是可选功能，主要用于：

- 一句话快速录入番剧记录
- 补充作品元数据
- 联网连通性测试

不配置 AI，也不会影响本地基础功能。

## 打包产物说明

当前 Windows 打包主要会生成两类可分发文件：

- 安装包：MSI / NSIS
- 便携包：ZIP

这些产物默认位于 src-tauri/target/release/bundle 下。

如果你是上传 GitHub Release，通常把这些产物作为 Release 附件上传即可，不建议直接提交到仓库源码里。